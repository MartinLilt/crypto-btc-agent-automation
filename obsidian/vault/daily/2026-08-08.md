# 2026-08-08

## Live grid-stream: report UX + bot rebrand

Live bot on Railway (EU, real ~$22 USDC) is healthy — first realized profit landed
after the `-1100` sci-notation qty fix earlier today. This session was polish, not
strategy.

### Cycle report reworked — commit `d0d2a59` (deployed, SUCCESS)

Cleaned up the per-cycle output (Telegram + stdout) now that trades actually realize:

- **Positions as a monospace, column-aligned table** (`Монета · Поз. · Влож · PnL% · Ждёт`)
  — wrapped in `<pre>` for Telegram, raw to logs. Emoji moved *out* of the table
  (double-width, broke alignment).
- `_fmt_price()` — compact prices across magnitudes (BTC `64,000` / ETH `2450.00` / XRP `1.0412`).
- **Order failures + reconcile drift split into their own `⚠️ Ошибки за цикл` section**
  (were mixed into the actions list).
- `_tax_line()` — dynamic Lithuania GPM estimate (15% above the €500/yr allowance),
  recomputed each cycle from current realized.
- Telegram message re-laid-out: per-coin trend as an emoji legend (🟢/⚪/🔴), explicit
  "без сделок" line when no trades fired.

Smoke-tested the helpers; auto-deployed from GitHub push, build clean. First new-format
message goes out on the next `0 */4` cron tick.

### Telegram bot rebrand — via Bot API, no code change

`@CryptoAgentTradeBot` still carried the OLD project's identity (name
"Crypto Agent | Spot Catcher", an AI/Revolut/cross-exchange description, dead menu
commands `/start /backtest /patterns /mode /status`). Updated to match the real
push-only grid-stream bot:

- **Name** → `🌊 Grid-Stream`
- **Overview + short description** → English, describing the automated spot bot
  (grid on pullbacks + hold in bull trend across BTC/ETH/BNB/SOL/XRP, 4h reports, 24/7).
- **Menu commands → cleared.** The app is a Railway cron push-notifier with no update
  listener, so any menu command was cosmetic/dead.

Notes: `@username` is BotFather-only (unchanged); overview shows only on the empty-chat
screen for new users. Documented in `logs/Dev Log.md`, committed `2e0528b`.

### State

Parking the project here for now. Live bot keeps running 24/7 on Railway; next natural
checkpoint is watching a `0 */4` cycle emit the new-format report and confirm the table /
tax line / errors section render cleanly in a real Telegram message.
