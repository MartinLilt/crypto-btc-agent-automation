# 2026-08-11

## Live check-up + strategy research session

Started from a user report of "errors + drawdown" in Telegram. Diagnosed, fixed
UX, then ran three strategy experiments and a sizing feature. Live bot healthy
throughout (Railway EU, real ~$21 USDC, cron `0 */4`).

### Shipped to live
- **reconcile drift** — the `⚠️ reconcile` line was a benign safe-direction dust
  warning (exchange > ledger, ~$0.55). Added a **$1 absolute floor** so sub-dollar
  dust on $5 bags stops spamming the report.
- **Dropped XRP** from the basket (`TARGET_COINS` = BTC/ETH/BNB/SOL) — had 0 bags,
  clean removal. Railway env + `.env` + config fallback.
- **Telegram trend/counts lines redesigned** — removed the confusing circles +
  legend; coins now grouped by state in words (`🧭 Тренд: все в боковике` /
  `📈 Растут: …`), counts de-noised to `🧺 Мешков · 💼 Холдов`.
- **`GRID_STEP_PCT` 0.5→0.8** — geometry sweep showed wider step keeps ~same equity
  while cutting drawdown 4.5%→3.7% and underwater-capital 5.3%→4.4%.
- **Dynamic bag-sizing** (`GRID_UNIT_PCT=0.004`) — bag = 0.4% of live balance,
  floor $6, auto-scales on deposit. No-op at current $22, scales once funded.
- **Deposit/withdrawal detection** — `💰 Пополнение` / `🏧 Вывод` line in the report.

### Research (negatives recorded, nothing shipped)
- **Idea #1 (pool TP — sell stack at avg cost):** WORSE (~45% less stream). The
  grid's edge is per-bag churn; pooling forgoes it.
- **Idea #2 (oversold entry gate — RSI/EMA):** WORSE (cuts stream more than risk).
  Deployment% dominates, not entry cleverness.
- **Geometry sweep + sizing test:** the real levers are step width + deployment %.
  Dynamic 0.4% sizing proven balance-invariant ($5k ≡ $10k in %); grid-sleeve-only
  sim understates the real regime-aware bot.

### State
Parking the crypto project. All committed/pushed (through `8fd3407`), tests 4/4,
live deployed. Open follow-up if revisited: full-runner backtest with bull-holds +
BEAR-no-buy for true equity numbers (offered, not built). Moving to another project.