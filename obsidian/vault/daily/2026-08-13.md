# 2026-08-13

## Live results deep-dive + report overhaul

User asked "как идут дела — мы в плюсе?". Read live Postgres directly and
reworked the Telegram report.

### Finding: strategy is net-positive; the "loss" was a ghost
- 8-day live realized read as **−0.15 USDC**, but **8 of 9 sells were clean
  grid-TP** (all small green, +0.7…+1.2% net of fees).
- The whole lifetime loss was **one artefact**: a phantom BNB bag from the
  08-05 paper→live flip, liquidated 08-08 at **−0.52 (−8.8%)**. The grid never
  voluntarily sells at a loss → this was a migration ghost, not the strategy.
- Since 08-09 **every** sell is green. Real edge is intact; absolute numbers are
  tiny only because capital is tiny (~$35, $6 bags).

### Shipped (report-only, no trading logic touched)
- **Second Telegram message** — win/loss ledger of the last 50 sells
  (date · coin · profit · % · ✅/❌) with a `🟢 N · 🔴 M · итог ±X` header.
- **`store.recent_sells(50)`** on both backends (Postgres + JSON).
- **Blunter headline block** for non-traders: `💰 Всего на счету`,
  `💵 Заработано всего ±X — ✅/❌`, `🎯 Если все закупки отработают`,
  `🛒 Открытых закупок`, `🧭 Рынок: спокойный/растёт/падает`.

### Ledger correction (explicit user request)
- Deleted the single phantom BNB sell (id=11, −0.5225) and backed its loss out
  of `portfolio.realized`: **−0.15 → +0.37 USDC**. 0 phantom rows remain;
  history now 7 green / 1 red / итог +0.37.

### State
Tests 4/4, both messages rendered against live data before shipping. Temp TCP
proxy used to reach Postgres, removed after. Committed + pushed → Railway deploy.

## Strategy research — EU-legal spot directions (nothing shipped)

Discussed how to grow small sums within the EU retail derivatives ban (which
kills the classic funding-carry that earlier research found best). Empirically
tested two directions with 2024-01-01→today Binance data (scratchpad scripts):

- **#4 depeg-harvest** (buy stablecoin dips, sell on re-peg): safe rule ≈
  **+3.7%/yr, budget-invariant** ($500 ≡ $10k in %). Iksy only in deep depegs
  (FDUSD Apr-25 0.87, TUSD Jun-25 0.91) = 2 events, a credit bet on issuer
  solvency, ceiling ~+15%, tail = total loss.
- **#3 liquidity/yield**: rebalancing premium (price core of LP) ≈ **<1%/yr**,
  negative on daily churn → vol-harvest as a price edge barely exists; LP return
  is almost entirely the fee subsidy. Realistic stable LP/lending ≈ 3-6%/yr.
- **Verdict:** both are 3-6%/yr yield sleeves, NOT iksy machines; #3 (stable
  yield) beats #4 on all sums. Real ceiling with a proven edge = **#1 momentum
  rotation** (offered, not yet run).

## Binance-policy audit of the live bot + efficiency fix

User: does the running system break any Binance policy? Pulled real limits/
filters, compared to actual (~3 orders/day). **Clean by 2-4 orders of magnitude**
on weight/orders/raw-requests. **Wash-trade impossible** (market-only, no resting
orders → no self-match). MIN_NOTIONAL $5 is the only tight filter — safe while
bag floor ≥ ~$5.5 (grid never sells underwater). Hygiene: live key already
`withdrawals=off, ipRestrict=on` (3 EU IPs) per the 08-05 log — user to eyeball-
verify. **Shipped (959f588):** per-cycle cache of `get_free_balances()` with
invalidation on fill; +2 tests → 6/6. Pushed → Railway.