# 2026-08-13

## Live results deep-dive + report overhaul

User asked "как идут дела — мы в плюсе?". Read live Postgres directly and
reworked the Telegram report.

### Finding: strategy is net-positive; the "loss" was a ghost
- 8-day live realized read as **−0.15 USDC**, but **8 of 9 sells were clean
  grid-TP** (all small green, +0.7…+1.2% net of fees).
- The whole lifetime loss was **one artefact**: a phantom BNB bag from the
  08-05 paper→live flip, liquidated 08-08 at **−0.52 (−8.8%)**. The grid never
  voluntarily sells at a loss → this was a migration ghost, not the strategy.
- Since 08-09 **every** sell is green. Real edge is intact; absolute numbers are
  tiny only because capital is tiny (~$35, $6 bags).

### Shipped (report-only, no trading logic touched)
- **Second Telegram message** — win/loss ledger of the last 50 sells
  (date · coin · profit · % · ✅/❌) with a `🟢 N · 🔴 M · итог ±X` header.
- **`store.recent_sells(50)`** on both backends (Postgres + JSON).
- **Blunter headline block** for non-traders: `💰 Всего на счету`,
  `💵 Заработано всего ±X — ✅/❌`, `🎯 Если все закупки отработают`,
  `🛒 Открытых закупок`, `🧭 Рынок: спокойный/растёт/падает`.

### Ledger correction (explicit user request)
- Deleted the single phantom BNB sell (id=11, −0.5225) and backed its loss out
  of `portfolio.realized`: **−0.15 → +0.37 USDC**. 0 phantom rows remain;
  history now 7 green / 1 red / итог +0.37.

### State
Tests 4/4, both messages rendered against live data before shipping. Temp TCP
proxy used to reach Postgres, removed after. Committed + pushed → Railway deploy.