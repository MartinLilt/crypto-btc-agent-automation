# 2026-08-14

## Multi-account shipped (Aleksand = observer for now)

Made the bot run for several traders, deployed it live. @AleksandLi joins as a
**watcher** — no keys yet, his slot doesn't trade.

### Shipped (commit 2a47d4a, Railway SUCCESS)
- **Per-account trading**: `config.Account` (slot/name/tg/keys) from env; account 1
  = primary (legacy keys, state slot 1), account 2 = slot 2, inactive until keyed.
- **State split**: portfolio by id-slot (live row = slot 1, untouched), new
  `trades.account_id` (migration verified on live DB — 23 rows preserved, id=1
  intact). Per-account signing keys + balance-cache clear on switch.
- **Whitelist DM reports** (user picked this over a shared group): `subscribers.py`
  + `_process_subscriptions()` — a user is registered ONLY if their @username is
  in `TELEGRAM_WHITELIST`, via /start; reports DM'd to all subscribers + owner.
- Backward-compatible; **tests 10/10**.
- Live env set: `TELEGRAM_WHITELIST=AleksandLi,limi_amm`, `ACCOUNT_1_TG=limi_amm`.
  No account-2 keys → observer-only.

### Next
- Both users message the bot once → next cron (12:00 UTC) registers their chat_id.
- Activate Aleksand later: his key (same 3 EU IPs, withdrawals OFF) → set
  `ACCOUNT_2_NAME/TG` + `BINANCE_API_KEY_2/SECRET_2`.

## €1000 replay of the live bot over 2026 (research)

User asked to run our actual bot on €1000, 2026 by month. Faithful full-runner
replay (grid + regime + bull-hold), scratchpad.
- Equity **€900 (−10% MTM)** in a year the basket fell **−33%** (BTC −28, ETH −37,
  BNB −30, SOL −39). Defensive design works: −10% vs −33% buy&hold.
- Realized **−€47.6** = grid **+€19.7** ✅ minus **bull-hold −€67.3** ❌ (overlay
  whipsawed on false BULL signals). → worth testing `BULL_HOLD_PCT=0`.
- Most of the MTM loss is unrealized frozen bags (recover on a bounce = the feast).