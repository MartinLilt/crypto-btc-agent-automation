# 2026-08-22

## Live-bot check
- Cron healthy, every 4h since the 08-17 deploy. Last cycle 04:03 UTC.
- equity **43.65 USDC (+17.96%)** on a 37.00 base · feast 39.91 · realized **+1.73**
- cash **2.11** → still **capital-starved**: unit 6.00 > cash, so no new bags and
  no BULL holds can be opened. All the upside is unrealized (ETH +31%, BNB +6%).
- regime: BTC:B · ETH:B · BNB:B · SOL:N

## The 00:02 UTC error — `-1013 Filter failure: NOTIONAL` on BTCUSDC
TP-sell of the BTC bag was rejected. Two causes stacked:
1. **Lot granularity vs bag size.** BTCUSDC step `0.00001` ≈ **$0.78** at 78k.
   A `quoteOrderQty=6.00` buy truncates DOWN to the step → bought **$5.40**, not $6.
   The sell truncates again → **$4.71 < $5 minNotional** → rejected.
2. **Float bug in `round_qty`.** `int(0.00007 * 100000)` == 6 → a qty exactly on
   the step lost a WHOLE step on every sell (BTC −$0.78, ETH −$0.25).

## Shipped
- `round_qty` floors with `Decimal` (exact, no phantom step loss).
- NOTIONAL filter cached; `market_sell` pre-checks it and fails locally with a
  readable message instead of a -1013 every cycle.
- `min_round_trip_unit = (minNotional + 2·step·price)·(1+fee)` → per-coin bag
  floor in `main.py`. Today: **BTC $6.58**, BNB $6.45, ETH $5.52, SOL $5.21.
- 3 regression tests added (13/13 pass).

## Open
- Stuck BTC bag (~$5.4): sellable under the fixed rounding only if the buy fee
  was paid in BNB. Otherwise → Binance Convert or a top-up.
- Local `.env` keys give 401 (IP-restricted to Railway EU) — can't read balances
  from this machine.
- Root problem is unchanged: **$37 of capital**. At $6 bags the book can barely move.
