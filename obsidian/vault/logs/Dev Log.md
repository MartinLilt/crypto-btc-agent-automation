# Dev Log

One entry per session / meaningful change. Newest at the bottom.

- **2026-07-30** — Bootstrapped project environment: venv, git, `.gitignore`, local `obsidian/vault` (daily + logs), CLAUDE.md. No application code yet.
- **2026-07-30** — Connected `origin` to `github.com/MartinLilt/crypto-btc-agent-automation` and force-pushed the local skeleton onto `main` (per explicit request), overwriting the previous 50-commit project. The overwritten history was preserved first on remote branch `backup/real-project-2026-07-30` (+ local tag `backup-real-project-2026-07-30`). Recover with `git push --force origin backup/real-project-2026-07-30:main`.
- **2026-07-30** — First MVP code: Binance market-data API module for LTC spot. Added `.env` + `.env.example` with Binance fields (`BINANCE_API_KEY/SECRET`, `BINANCE_TESTNET`, `SYMBOL=LTCUSDT`, `INTERVAL`, `CANDLE_LIMIT`, `QUOTE_ORDER_QTY`). New `src/config.py` (loads .env via python-dotenv) and `src/binance_api.py` (public REST wrapper: `get_price`, `get_candles`→typed `Candle` OHLCV, `get_24h_stats`; no keys needed for market data). `main.py` demo prints price/24h stats/candle range. Deps: `requests`, `python-dotenv`. Verified live: pulled LTCUSDT price 45.22 + 200×1h candles.
- **2026-07-30** — Spot MVP trading loop (paper-first). User added Binance keys; discovered they are **REAL account keys** (200 on api.binance.com, 401 on testnet) with **withdrawals ENABLED** — flagged as a security risk, advised disabling. Chose to build our own **paper mode** rather than use Binance testnet. Added `TRADING_MODE=paper|live` + SMA/fee/paper-balance fields to `.env`/`.env.example`. New `src/strategy.py` (SMA crossover → BUY/SELL/HOLD `Signal`), `src/broker.py` (`PaperBroker` simulates on virtual balance persisted to `state/paper_state.json`; `LiveBroker` places real signed market orders with LOT_SIZE rounding; `execute()` enforces single-position — no double buy/sell). `main.py` is now the runner: candles → signal → broker → report. `state/` gitignored. Verified: paper BUY/SELL round-trip works, double-BUY ignored, state persists. Still paper-only — not trading live yet.
- **2026-07-30** — Added `backtest/` package: `engine.py` (`run_backtest` walks candles forward, feeds each history slice to the SAME `src.strategy.evaluate`, simulates buys/sells with identical accounting to PaperBroker; reports return %, buy&hold benchmark, closed trades, win rate, max drawdown) + `run_backtest.py` CLI (`python -m backtest.run_backtest [interval] [limit] [fast] [slow]`). Uses real Binance history, never trades. Results: 1h/1000 → +0.08% (buy&hold +1.71%); 1d/1000 (2023→2026) → +1.04% vs buy&hold −37% (strategy sits flat through downtrends). Returns small because QUOTE_ORDER_QTY=20 deploys only ~2% of the 1000 balance per trade. Per user: **live mode is user-only — assistant will not enable TRADING_MODE=live.**
- **2026-07-30** — Added `backtest/optimize.py` grid search: sweeps FAST_GRID×SLOW_GRID (fast<slow) over one candle load, ranks by return|sharpe|winrate. `python -m backtest.optimize [interval] [limit] [metric]`. Findings: 1d/1000 best = SMA 20/50 (+1.04% vs buy&hold −37%); 1h/1000 best by sharpe = SMA 25/50 (+0.16%). Honest read: strategy's value on LTC is **downside protection in bear regimes, not alpha** — raw returns are tiny (partly 2% sizing, partly weak edge). Warned in output to re-validate best params on a DIFFERENT period (overfitting risk).
- **2026-07-30** — Refactored `src/strategy.py` into a **3-strategy registry with a switcher** (`STRATEGY=sma|ema|rsi` in `.env`, mirrors the paper/live switch): `sma` (SMA crossover), `ema` (EMA crossover, faster), `rsi` (reversion: buy leaving oversold, sell leaving overbought). Generalized `StrategyResult` to `{signal, reason, indicators}`; each strategy carries `default_params`, `param_grid`, `warmup`. Threaded the switch through config, `main.py` runner (prints active strategy + its indicators), and made `backtest/engine.py` + `run_backtest.py` + `optimize.py` strategy-aware (optimizer sweeps the chosen strategy's own grid). Verified all 3 in runner + backtest. On LTC 1d/1000: sma +1.04%, ema −1.76% (whipsawed, 24 trades), rsi −0.67% — all beat buy&hold (~−37%) by staying flat in the downtrend; sma remains the least-bad. EMA/RSI raw edge on LTC is weak.
- **2026-07-30** — 6-month test on **LTCEUR** daily (2026-02-01→07-30, start 1000 €, all-in per trade). Period was bearish (LTC 49.33→39.5 €, buy&hold −15.15%). **None went into profit** — result: RSI −8.94% (910 €), SMA −9.89% (901 €), EMA −15.46% (845 €, whipsawed on 4 trades). RSI & SMA preserved capital better than holding; EMA did not. Conclusion reinforced: long-only spot on LTC can't profit while the asset falls — these strategies limit losses, they don't create gains in a downtrend. A profit test needs a flat/up period or a different instrument.
- **2026-07-30** — Added **market regime (tendency) detector** `src/regime.py` — the planned 3rd axis (mode × strategy × regime). `detect_regime()` classifies BULL/NEUTRAL/BEAR from a long SMA (REGIME_MA=50) slope over REGIME_SLOPE_LOOKBACK=10 candles with a ±REGIME_FLAT_PCT=2% flat band + price-vs-MA confirmation. Config + .env params added; runner (`main.py`) now prints regime BEFORE the signal. Validated on 6mo LTCEUR daily: 0% BULL / 68% NEUTRAL / 32% BEAR — correctly found zero uptrend in a −20% period (BEAR segment 06-06→07-13). NEXT: the 3×3 behaviour matrix (how sma/ema/rsi each act per regime) — awaiting user confirmation of the gating policy.
- **2026-07-30** — Implemented the **3×3 regime→behaviour matrix** as gating layer `src/policy.py` (`decide()` = regime + strategy → gated signal). User-confirmed policy: BEAR = no buys + exit to cash (all strategies); each strategy active only in its zone (sma: BULL+NEUTRAL; ema: BULL; rsi: NEUTRAL; else stand-aside=block buys/allow exit). Zones: active/aside/cash. Added `REGIME_GATE` on/off switch; `decide()` threaded through runner + backtest engine (with `warmup_override` for fair A/B). **Fair aligned A/B on 6mo LTCEUR (1000€ all-in, common start):** SMA −10.33%→−10.33% (no change), EMA −7.66%→**0.00%** (gate kept it in cash — win), RSI −8.94%→**−19.58%** (gate HURT it). Root cause traced: RSI bought 05-31@44.64; gate forced BEAR exit 06-06@35.97 (at the bottom, pre-bounce), vs ungated hold→sell 07-24@40.73. **Insight: forcing a mean-reversion strategy to cash-exit on BEAR sells at the dip — the opposite of its design.** Recommend refining RSI's BEAR cell from 'cash' to 'aside' (hold, block new buys). Awaiting user decision.
- **2026-07-30** — User approved: **RSI BEAR = aside** (not cash). Final matrix — sma: active/active/cash, ema: active/aside/cash, rsi: aside/active/aside. Re-ran aligned A/B on 6mo LTCEUR (1000€): SMA −10.35%→−10.35%, EMA −7.68%→**0.00%**, RSI −8.94%→**−8.94%**. Result: regime gate now **helps EMA, harms nothing** (RSI no longer force-sold at the dip); all beat buy&hold (−15%). Regime layer complete: 3 switches live (TRADING_MODE × STRATEGY × REGIME_GATE). Still paper-only.
- **2026-07-30** — 1-YEAR test LTCEUR daily (2025-07-31→2026-07-30, 1000€ all-in). Brutal bear year: price 93.02→39.28 (buy&hold −56.88%), regime mix 0% BULL / 44% NEUTRAL / 56% BEAR. **Regime gate proved its worth:** EMA −47.49%→**0.00%** (stayed fully in cash), RSI −32.93%→**−8.94%**, SMA −14.69%→−15.38% (flat). Gated results all crush buy&hold (−57%). But still no profit — because there was **literally 0% BULL regime** in the whole year; LTC only fell across every window tested (6mo & 1yr). Conclusion: strategies + gate work (turn catastrophe into small loss/breakeven), but a PROFIT demo needs a period/asset that actually trended up — LTC hasn't in the last year.
- **2026-07-30** — Committed to **short-term spot only** (STRATEGY=rsi, INTERVAL=1h in .env; no long holds). Added **cost modelling**: Binance fee 0.1%/side (tracked as `total_fees`) + **Lithuania tax** (TAX_RATE=0.15 GPM, TAX_ALLOWANCE=500€/yr) → `BacktestResult.tax()/net_equity()/net_return_pct()` compute take-home after fees AND tax (tax only on gains above allowance). Added **paginated candle fetch** (`get_candles` walks endTime backwards for >1000 bars). **1-YEAR 1h LTCEUR (8760 bars, 1000€ all-in):** price 94.48→39.30, buy&hold −57.63%. Gated: SMA −53.81% (106 trades, fees 127€), RSI −26.40% (102 trades, 55% win, fees 183€), EMA 0% (0 trades). **TWO KEY FINDINGS:** (1) regime gate is **timeframe-miscalibrated** — REGIME_MA=50 on 1h = only ~2 days of history, so it read **97% NEUTRAL** all year and gave almost no bear protection; regime must be measured on a HIGHER timeframe (e.g. daily) than the trading TF. (2) **fees are a huge drag on short-term** — 100+ trades cost 13–18% of capital. RSI still least-bad (55% win cut −58% to −26%). Tax=0 (no profits). NET machinery verified. NEXT: user will add another indicator.
- **2026-07-30** — Added **target-coin selector** `src/screener.py` — new Layer 0 of the pipeline (target → mode → regime → strategy). `screen()` runs OUR engine (strategy + gate, net of fees+tax) over each candidate in TARGET_COINS across recent candles and ranks by net return (one number capturing trend+volatility+fit); `pick_target()` returns best if net>0 else None (stay in cash). CLI `python -m backtest.screen [interval] [limit] [strategy]`. Config: TARGET_COINS (10-coin universe), AUTO_TARGET (runner auto-picks & overrides SYMBOL), TARGET_SCREEN_LIMIT=720. Wired into `main.py` (Layer 0). Fixed hardcoded 'LTC' label → derives base asset from symbol. **Screen result (RSI/1h/~30d): AVAX +8.52% net (winner), ETH +4.89%, BTC +3.63%, LTC only +1.57%** — data confirms user's read that LTC is weak; system would rather trade AVAX/ETH/BTC. If all net≤0, screener says stay in cash.
- **2026-07-30** — 1-YEAR AVAXUSDT 1h (8760 bars, 1000 all-in, net): price 23.30→6.47, **buy&hold −70.18%** (AVAX was the WORST faller of the whole universe over the year). Gated net: SMA −61.27% (105 trades), RSI −37.11% (113 trades, 62% win), EMA −4.12% (2 trades). **Two hard lessons:** (1) **Screener recency bias** — AVAX ranked #1 on the last ~30 days (+8.5%, a dead-cat bounce) but was −70% over the year. Ranking on a short recent window picks bounces in long downtrends. Fix: screen on a longer lookback and/or require a confirmed higher-TF uptrend. (2) **Regime gate still broken on 1h** (94% NEUTRAL) → no bear protection → SMA/RSI traded straight through the −70% crash. The multi-timeframe regime fix (regime from daily, trade on 1h) is now the critical blocker — unprotected 1h short-term trading is dangerous. Whole crypto space stayed bearish; no long-only spot profit available in this window.
- **2026-07-30** — **Refined the coin selector to be JUSTIFIED** (fix recency bias). Now judges on a LONG daily window (TARGET_SCREEN_INTERVAL=1d, TARGET_SCREEN_LIMIT=365) with three trend gates: (1) daily regime not BEAR, (2) window momentum > 0, (3) rose in a majority of sub-periods (consistency, kills dead-cat bounces). Ranked by momentum; system net shown as context (daily-RSI is quiet so net isn't the ranker — the real edge is confirmed by backtesting the pick on 1h). Config: TARGET_SCREEN_INTERVAL added, LIMIT default 720→365. Results: **1yr — NOTHING qualifies** (all coins −25%…−78%; AVAX now ranked near-LAST, was #1 in the buggy version) → runner stays in cash (verified). **120d — BNB qualifies** (momentum +0.82%, uptrend 100% of sub-periods) proving it's not always-cash; it picks the steadiest recent uptrend, not the biggest bouncer. Coin selection now sound.
- **2026-07-30** — **Fixed multi-timeframe regime gate — full pipeline now operational.** Added REGIME_INTERVAL=1d (measure trend on a higher TF than we trade). `src/regime.py`: `tf_factor()` + `resample()` (aggregate N trading bars → 1 regime bar). `policy.decide()` gets `regime_override`. Engine precomputes a no-lookahead `_regime_timeline()` (regime of last COMPLETED higher-TF bar mapped onto each trading bar) and warmup scales by factor. Runner fetches daily regime candles separately. **Proof on 1yr AVAX 1h (buy&hold −70%):** regime detection went from blind (single-TF 1h saw 15 BEAR / 340 NEUTRAL) to correct (multi-TF 1d saw 178 BEAR / 114 NEUTRAL / 12 BULL). Trading net improved for the active strategies: SMA −62.9%→−46.1% (89→37 trades), RSI −51.4%→−38.4% (97→36); EMA −4.1%→−12.5% (barely trades, caught a few false-dawn BULL entries). Gate now gives real bear protection on short TF. Still all-negative because AVAX fell 70% with only 12 BULL days — but damage cut ~15pts. Pipeline complete: 🎯target → mode → regime(multi-TF) → strategy → net(fees+tax). Verified runner end-to-end. Paper-only.
- **2026-07-30** — Added **opportunity scanner** `backtest/opportunities.py` — sweeps coins × timeframes(1h/4h/1d) × strategies, gated+multi-TF, net of fees+tax, reports every profitable combo. Fixed engine crash when warmup≥len (return flat result). **FIRST REAL PROFIT POCKETS found in the declining market: 10/90 combos positive, 6/10 coins have a profitable setup.** Standouts (NET after Binance fees + LT 15% tax): **BNB 4h RSI +15.27% (86% win, buy&hold −5%)** ← best; BNB 1h SMA +7.85%; DOGE 4h SMA +4.94% (while DOGE itself fell −27.6% — extracted profit from a falling coin by catching bounces + sitting out crashes). **4h is the sweet spot** for short-term (enough movement, less fee churn than 1h). Caveat: 90-combo sweep → overfitting risk; best pockets need out-of-sample validation. But proves the system CAN earn in a down market via short-term reversion. Paper-only.
- **2026-07-30** — **Made the pipeline AUTONOMOUS** (`src/autopilot.py`). Per user: system self-selects coin → tendency → strategy; only TRADING_MODE stays manual. `select_strategy(coin)` backtests all 3 strategies (gated, multi-TF, net) and returns the best with net>0 AND ≥3 trades (anti-fluke). `plan()`: with AUTO_TARGET, scans the whole universe, picks the coin+strategy combo we're most PROFITABLE on (NOT a plain uptrend filter — reversion earns in flat/down markets, so this finds pockets the trend-screener rejected); reads that coin's multi-TF regime; else cash. Config: AUTO_STRATEGY flag; enabled AUTO_TARGET+AUTO_STRATEGY; set INTERVAL=4h (proven sweet spot + fast: 10 coins×3 strat in ~8s). `main.py` rewritten around `plan()`. **Verified end-to-end: autopilot autonomously chose BNBUSDT + RSI (backtest +12.94% net, 83% win), tendency NEUTRAL, ran on 4h paper.** Note: coin selection uses recent-window net (overfitting risk remains) — out-of-sample validation still pending. Paper-only; live is user-only.
- **2026-07-30** — **OUT-OF-SAMPLE (walk-forward) validation — the reality check.** `backtest/walkforward.py`: anchored walk-forward — SELECT best coin+strategy on a training window (= autopilot), TEST it on the next unseen window; OOS return sliced from the equity curve (added `warmup` to BacktestResult for alignment). **Verdict across configs: the autonomous edge does NOT generalize.** 4h/37d-seg: 1/3 OOS+, avg −1.60%. 4h/60d-seg: 1/3, avg +0.15% (noise). 1d/90d-seg: **0/4 OOS+** — SMA BNB IS +21.34% → OOS −3.01%. Consistent signature: strong in-sample (+6…+21%) collapses to break-even/negative out-of-sample = **overfit; simple SMA/EMA/RSI have no durable predictive edge on these liquid coins.** The system's genuine value is capital PROTECTION (cash default / loss-cutting), not alpha. **DO NOT go live expecting profit.** Real edge would need a different signal class (cf. [[research_strategy_search_2026_05_17]] rotation+funding-carry). Well-built framework, honestly negative result.
- **2026-07-30** — **Strengthened the toolchain (user asked for all 4 directions), then re-validated OOS.** Built: (1) **exit management** `src/exits.py` — TP/SL/trailing/timeout, checked every bar before the strategy, wired into engine (added `_sell` helper, entry_price/peak/bars_held tracking) + config/.env; (2) **new signal class** `breakout` — Donchian volatility breakout (buy N-bar high, sell N-bar low), added to registry + policy matrix (active/active/cash); (3) **OOS-robust selection** — `autopilot._robust_eval` scores by REPEATED edge (net>0 overall + majority of sub-periods + recent sub-period positive + ≥4 trades), ranks by avg sub-period return, not raw recent net; walkforward now tests this same method. **HONEST OOS VERDICT — enhancements did NOT create edge:** robust+breakout no-exits 1/3 folds, avg +0.48%; +SL3/trail4 1/3 avg +0.15%; +SL2/TP6 0/3 avg −0.76%; +trail3 0/3 avg −1.43%. Every config sits at break-even-to-negative OOS; exits generally neutral-to-worse (extra params overfit; trailing cuts winners in chop). **Confirmed: no amount of smarter selection/exits manufactures alpha from TA noise on liquid coins.** Stopped tuning (would just data-dredge the validator). Framework now feature-complete & sound; edge still absent. Only untried path with theoretical basis: a different SIGNAL class (funding-carry / cross-sectional relative-strength rotation), not more TA. Paper-only.
- **2026-07-30** — **Built funding-carry — the first REAL (structural, non-overfit) edge.** `src/funding.py` (public Binance USD-M `/fapi/v1/fundingRate` + `premiumIndex`, no keys; 8h funding, annualize helpers) + `backtest/funding_carry.py` (delta-neutral carry = long spot + short perp; realized net = Σfunding − 4×fee round-trip, annualized; ranks coins). **Result (trailing ~167d, net of fees): LINK +3.0%/yr (76% of periods positive), LTC +1.18%, DOGE +1.08%, BTC +0.71%, AVAX +0.12% carry-worthy; ETH/ADA/XRP/SOL negative.** Thin NOW because bear market = low long demand compresses funding — but it's POSITIVE and STABLE (structural yield, not a coin-flip), the opposite of TA's noise. Current funding much higher than trailing avg (BTC/LINK ~+10-13%/yr now) → carry improving, scales to 20-50%/yr in bull regimes. Key contrast: TA = +0.48% OOS unreliable noise; carry = small but REAL, consistent, regime-scaling. Execution needs a futures short leg (beyond pure spot). Data+math done; not yet wired into runner/autopilot.
- **2026-07-30** — **MVP scope fixed to short-term spot LONGS** (funding-carry parked for later — needs futures leg). Wired **exit management into the live/paper runner** so "longs" stay short: extended `Position` with entry_price/peak_price/entry_epoch (persisted in paper_state.json), `PaperBroker.buy` stamps them, added `update_peak()`; `main.py` now checks TP/SL/trailing/timeout BEFORE the strategy each cycle and forces SELL on a fired rule. Exit defaults set for 4h: TAKE_PROFIT=5%, STOP_LOSS=3%, MAX_HOLD_BARS=18 (~3d). Verified: stop-loss fires at −4%; autopilot end-to-end picks coin+strategy, regime-gates, exits enforced. MVP is now a complete autonomous short-long paper bot (coin→tendency→strategy→exits, net fees+tax; MODE manual). Ready for forward paper-testing. Honest caveat still stands (TA edge weak OOS) — this is for working/testing, not proven profit.
- **2026-07-31** — **Dynamic coin universe** (user: "take more coins"). `binance_api.get_top_symbols` (top-N spot by 24h quote volume, skips leveraged tokens + stables/fiat + gold XAUT/PAXG), `funding.get_perpetual_symbols` (USD-M perps set), `src/universe.get_universe` (AUTO_UNIVERSE=true → wide top-volume list ∩ perps → top UNIVERSE_SIZE; cached per run; falls back to TARGET_COINS). Wired into autopilot + screener + all backtest CLIs. Config: AUTO_UNIVERSE, UNIVERSE_SIZE=30, MIN_QUOTE_VOLUME=10M. Verified: yields 29-30 real liquid crypto (BTC/ETH/SOL/…/AAVE/NEAR/SUI/ENA). Autopilot on 30 coins ~20s. **DOUBLE-EDGED — as predicted:** wider universe picked small-cap BANKUSDT RSI +24.72% in-sample — a classic recency/overfit red flag (more candidates = more multiple-comparisons noise), NOT a green light. robust-score gates but doesn't eliminate it. Recommend keeping MIN_QUOTE_VOLUME high + extra skepticism on small-cap picks; the OOS verdict (TA no durable edge) is unchanged by more coins.
- **2026-08-01** — Raised MIN_QUOTE_VOLUME to 20M (15-coin universe; small-caps without enough 4h history auto-drop, ~10 tested). Wired walkforward to use `get_universe()`. **Walk-forward across universe — the discipline lesson:** 3-fold/360-seg looked great (2/3 OOS+, avg +6.71%, "GENERALIZES") — but that was SEGMENTATION LUCK, driven by one fold (BREAKOUT ZEC +20.91% OOS). Re-ran: 4-fold/260 → 1/4, avg +0.49%; 5-fold/210 → 1/5, avg −0.72%, and the same ZEC BREAKOUT that looked amazing (IS +50.34%) collapsed to OOS −4.56%. **Conclusion: the wider universe did NOT create durable edge — it just produced more lucky-looking single runs. Averaged across fold nestings it reverts to the standing verdict: TA selection ≈ break-even/negative OOS, no durable edge.** Reinforces: never trust one backtest/one walk-forward; check robustness across configs. Real edge remains funding-carry, not more coins.
- **2026-08-03** — **Micro-scalp / grid simulator** `backtest/scalp.py` (multi-position, MARK-TO-MARKET equity so frozen bags show real unrealised loss). Computed fee/tax floor: round-trip fee 0.2%, so min viable TP ≈ +0.55% gross to net +0.3% after LT tax — true "micro" (<0.2%) is impossible. Tested 3 modes on AVAX(−28%) & BTC(−6%) 4h: **A) scalp+stop** — controlled, realises losses (AVAX −10.9%, BTC −4.2%). **B) grid/freeze (no stop)** — the user's bag-and-accumulate idea: shows POSITIVE realised cash "stream" (+184/+99) but mark-to-market −15%/−6.7% with 10-11 frozen bags and cash exhausted (84/99 left) — the pennies-before-steamroller trap made visible. **C) grid/freeze + UPTREND filter** (bags only when price>SMA100) — the fix: AVAX −15.2%→−7.2%, BTC −6.7%→**−0.27%** (near-flat while BTC fell 6%), cash preserved (556), drawdown halved. Regime-BEAR filter did nothing (slow grinds read as NEUTRAL) — the price>SMA trend gate is what worked. Best result yet for the "money stream" goal, BUT still negative absolute (no up/ranging market in the data yet) and unvalidated OOS. Next: test on a genuinely ranging/up market + walk-forward.
- **2026-08-03** — **Grid+trend on blue-chips = first robust, positive result of the whole project.** User's strategy: adapt to the Trump-era bear, live off the grid "stream" now, feast when bags recover in the next bull cycle; accepts bag accumulation. Honest safeguard added: bag ONLY blue-chips (BTC/ETH) where recovery is reliable — many alts never return. Added `recovery_equity` to scalp sim (payoff if every frozen bag hits its TP = the "feast"). Mode C (grid+SMA100 uptrend filter) on Feb–Aug 2026: **ETH +2.36% mark-to-market while ETH itself −5.2%** (realized +98, feast +10%); BTC −0.28% vs −6.3% (feast +5.9%). **Robustness sweep (27 param combos tp×step×sma): ETH ALL 27 positive (+1.2…+8.3%), BTC all near-flat with ALWAYS-positive realized stream (+35…+98) and feast (+4…+10%).** Unlike TA (fell apart across configs), grid+trend holds across the whole parameter space — because it HARVESTS VOLATILITY + avoids falling knives (structural, like carry), rather than PREDICTING direction. Tradeoff: tighter grid = bigger stream but higher drawdown (mdd 5–18%). Caveats: one time window so far (need other periods); mark-to-market depends on end price; capital locks in bags; only safe on blue-chips that recover. This is the money-stream candidate. Not yet wired into runner (needs multi-position grid broker).
- **2026-08-03** — **Grid-suitability analyzer** `backtest/grid_screen.py` — runs mode C per coin over the FULL Trump-era bear (from ~2025-01-13, ~3400×4h bars), scores stream/equity/feast/drawdown, flags blue-chip (reliable recovery) vs ⚠ risky. **Sobering reality vs the kind Feb–Aug window:** over the full 18-month bear, blue-chip grid does NOT preserve capital — stream always positive (+131…+390) but mark-to-market deeply negative (BNB −13%, ETH −26%, SOL −39%, BTC −32%, ADA −73%) with brutal drawdowns (35–82%). The SMA100 trend filter isn't enough over 18mo of relentless decline. ZEC (coin +904%, grid +202%) is the proof-of-feast when a coin turns bullish. **Conclusion: the strategy is fundamentally a bet on eventual recovery** — real stream + big feast (+34–40% on ETH/SOL/BNB) but you must survive −40–70% mark-to-market to reach it. Strategy-fit picks (blue-chip · stream · feast, drawdown accepted by the bag-hold thesis): SOL/ETH/BNB/XRP/ADA; BNB best risk/reward (mildest 35% drawdown). Analyzer done — next: refactor pipeline for the grid premise.
- **2026-08-03** — **System named "Grid-stream"** (grid harvests a stream now, bags accumulate in the bear, feast on the bull reversal). Locked the bear-cycle coin policy: **AUTO_UNIVERSE=false** during the bear (it pulls small-caps whose bags may never recover), curated blue-chip **TARGET_COINS=BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,XRPUSDT** (dropped ADA — 82% drawdown + slower recovery; dropped LTC/DOGE/LINK/AVAX and all small-caps for now). Core = BTC/ETH/BNB/SOL (BNB best risk/reward, BTC most reliable recovery), XRP optional. Rationale: only coins whose bags reliably "startanut" in the next cycle. Widen the universe again in a bull cycle. Applied to .env + .env.example. Next: refactor the pipeline (multi-position grid broker) under the Grid-stream premise.
- **2026-08-03** — **Pipeline refactored to Grid-stream.** New: `src/grid.py` (pure core — `is_uptrend`, `bags_hitting_tp`, `should_add_bag`; no stop by design), `src/grid_broker.py` (multi-bag paper broker — shared cash pool, bags per coin, mark-to-market equity + feast valuation, persists to state/grid_state.json), `main.py` rewritten as the Grid-stream runner: each cycle loops the basket, sells bags at micro-TP (the stream drips), adds a bag only when price>SMA (uptrend) on a deeper dip. Config grid params: GRID_TP_PCT=0.8, GRID_STEP_PCT=1.5, GRID_UNIT_USDT=40, GRID_MAX_BAGS=8, GRID_SMA=100. Old single-position autopilot/policy/exits/broker kept in repo but off the main path. Verified paper end-to-end: on the basket, BTC/ETH/SOL/XRP below SMA100 → no bags (correct), BNB uptrend → opened 1 bag; state persists across runs (no dupes). Runner is stateless-per-cycle → ready to run on a schedule (cron ~4h) as a live paper forward-test. Live grid execution (many real orders/coin) not wired — paper only, user-gated.
- **2026-08-03** — **grid_screen control on the curated basket with LIVE config.** Added `max_bags` cap to `run_grid`; aligned `grid_screen` to pull live params (`params_from_config`: TP 0.8/step 1.5/SMA100/unit 40/max 8). **The bag cap transforms the risk profile** vs the earlier uncapped test: over the full 18mo bear, equity BNB −4.3% (coin −13.5%), ETH −7.0% (−38.8%), SOL −9.2% (−59.1%), BTC −10.6% (−31.5%), XRP −15.2% (−56.5%) — drawdowns now 14–20% (was 35–82%). All 5 blue-chips preserved capital FAR better than holding + dripped a positive stream (+39…+124) + carry feast upside (+4…+13%). Confirms curated basket + live config (unit 40, max 8) is well-calibrated: conservative, survivable, beats buy&hold across the bear. Rank: SOL/ETH/BNB best. Grid-stream MVP validated on the real universe.
- **2026-08-03** — **Deployed to Railway.** Adapted for cloud: `STATE_DIR` env (grid_broker/broker write there) so state survives on a mounted volume; `railway.json` (NIXPACKS, startCommand `python main.py`, cronSchedule `0 */4 * * *`, restart NEVER); `.python-version` 3.12. Committed clean (Claude co-author permanently disabled via ~/.claude/settings.json includeCoAuthoredBy=false) + pushed (ab24bf4). Railway: project crypto-btc-agent-automation (bfd13fd1…), service grid-stream (35312f73…) in production, volume mounted at /data, env vars set (paper config: TRADING_MODE=paper, TARGET_COINS blue-chips, GRID_*, STATE_DIR=/data). **Binance keys deliberately NOT set** — paper doesn't need them and they still have withdrawals enabled; add only when going live (after disabling withdrawal). Deploy SUCCESS, volume attached; verified via `railway run` that main.py runs correctly with Railway env (basket + grid params + BNB↑ bag). Cron waits for next 4h tick. **CI/CD** via GitHub Actions (`.github/workflows/railway-deploy.yml`): push to main → `railway up` redeploy; one-time user step = add repo secret RAILWAY_TOKEN (Railway project token).
- **2026-08-03** — **CI/CD switched to native Railway↔GitHub integration** (project-token creation was blocked for the OAuth session, and it's simpler). User connected the repo in the Railway dashboard; service grid-stream source = MartinLilt/crypto-btc-agent-automation. Dropped the GitHub Actions workflow (`railway-deploy.yml`). **Verified end-to-end:** push d6af8dd → Railway auto-built & deployed from GitHub, latest deployment SUCCESS with matching commit. CI/CD live: push to main → auto-deploy; railway.json cron (0 */4) + /data volume carry over. Deploy fully operational; paper-only, Binance keys still not in cloud.
- **2026-08-03** — **Telegram results output.** `src/notify.py` (Bot API sendMessage, no-op if unconfigured, never raises); `main.py` sends a per-cycle results summary (ручеёк/equity/feast/bags + actions). Config: TELEGRAM_BOT_TOKEN/TELEGRAM_CHAT_ID. Bot @CryptoAgentTradeBot ("Crypto Agent | Spot Catcher"); chat_id 705456139 (private, @limi_amm) resolved via getUpdates. Token in .env (gitignored) + Railway vars — NOT in repo. Verified send OK. Railway cron will now push results to Telegram every 4h.
- **2026-08-03** — **CRITICAL forward-sim finding: continuous full-year 2025 backtest exposes the bag-accumulation trap.** Per-MONTH tests (independent, balance reset) flattered Grid-stream (+3%/mo stream, losses < market). But the CONTINUOUS 2025 run (bags carry over, real behavior) on the basket: basket buy&hold −8.5%, **Grid-stream equity −16.0% (WORSE than holding), cash exhausted ($6/$1000), 34 frozen bags, maxDD 40.8%.** Realized stream +$365 (+36.5%) got recycled into new bags that sank; by Nov/Dec the stream DRIED to $0 (no cash left, bags underwater, none reaching TP). The +37% "feast" is entirely locked in unrecovered bags. Confirms the early warning: in a SUSTAINED bear the grid-freeze is a capital-burial machine — it underperforms passive holding and locks all capital. The monthly view was misleading (reset hid accumulation). Grid-stream only works with free cash + a market that bounces; needs a cash-reserve / total-bag cap / bear cutoff, or it's a leveraged bet on recovery. Honest negative on the full-year continuous test.
- **2026-08-03** — **Order-granularity test (2025 continuous, $10k): smaller/more orders help a lot — via lower capital deployment, not order count.** BASE (unit400/step1.5/max8): +$3656 stream, −16.0% equity, 40.8% DD, $445 fees. MORE (unit100/step0.75/max20): −5.74%, 22% DD. MANY (unit40/step0.5/max40): +$1017, −3.23%, 11.8% DD, $155 fees. MAX (unit15/step0.35/max80): +$422, −1.35%, 5.1% DD, $70 fees. **Insight: tiny units deploy far LESS total capital (MAX put only ~$2k of $10k to work, $8k stayed cash) → drawdown collapses, but the stream shrinks proportionally. It's a risk dial (deploy% ↔ stream vs drawdown), effectively an auto cash-reserve — NOT free profit.** Corrected my prior guess: fees track VOLUME not order count, so smaller configs paid LESS fee ($445→$70). Sweet spot ~MANY: +$1017 stream, −3.23% (beats hold −8.5%), 12% DD. Confirms the real lever is capital-deployment cap / cash reserve.
- **2026-08-03** — **Applied the MANY (risk-controlled) config to the live bot.** Changed GRID params: TP 0.8→0.6, STEP 1.5→0.5, UNIT 40→4 (≈0.4% of the $1000 paper balance), MAX_BAGS 8→40. This deploys far less capital (auto cash-reserve) → 2025-bear drawdown ~12% vs 40% and equity ~−3% vs −16%, with a steady smaller stream. Updated .env + .env.example + Railway vars, reset grid state, verified local run ($4 bags), committed a56fbfd + pushed → CI/CD auto-deploys. Caveat noted: for LIVE, unit must be ≥ Binance min-notional (~$10), so live needs balance ≥ ~$2500 to keep the 0.4% ratio.
- **2026-08-03** — **Set Binance-real order size: $10/bag on a $2500 balance** (unit=0.40% → keeps the safe MANY profile with live-legal order size). GRID_UNIT_USDT=10, PAPER_START_BALANCE=2500. 2025-bear sim at this config: stream +$254, equity −3.23%, feast +10.4%, maxDD 11.8%. Updated .env/.env.example/Railway, reset state (fresh $2500), verified ($10 bag, cash 2490), committed 0bf51a8 + pushed → CI/CD deploys. Bot now paper-trades $10 orders like real Binance.
- **2026-08-03** — **Regime detector wired into the live bot + adaptive strategy.** Reused `src/regime.py` (BULL/NEUTRAL/BEAR, multi-TF via resample) as the market-state instrument; `grid.plan_actions()` switches behaviour; runner shows per-coin regime + Telegram line. **Validated adaptive vs plain grid ($50k, unit$200): full version (bull-hold + bear-no-buy) — 2025 −0.19% (great) but 2024 +9.35% vs +12.87% (bull-hold HURT). Bear-defense only — 2025 −2.87% vs −3.23%, DD 10.4% vs 11.8%; 2024 +11.65% vs +12.87%.** Honest finding: regime tweaks are MARGINAL/mixed — grid performance is dominated by capital-deployment %, not regime rules, and NO grid variant rides a strong bull (deploys too little). Shipped the clean version: BEAR=no new bags (defense, lowers DD), NEUTRAL/BULL=normal grid; dropped bull-hold (it cut profitable churn). REGIME_ADAPTIVE flag. The regime detector is valuable as an INSTRUMENT (info/display); true bull-capture needs a separate buy-&-hold allocation, not a grid tweak — flagged as next. Committed 96267d3, deployed.
- **2026-08-03** — **BULL mode built: buy-&-hold allocation to ride trends** (fixes the weak phase). In a coin's BULL regime the bot deploys BULL_HOLD_PCT of capital into a HOLD position (no grid, no micro-TP) and rides; when the coin leaves BULL it liquidates the hold (takes the trend gains) and resumes adaptive grid. `grid_broker` gained holds (buy_hold/sell_hold/hold_value, persisted, included in equity + feast). **Backtest ($50k, unit$200) — big win: 2024 bull +11.65%→+36.15% at 20% hold; 2025 bear −2.87%→+13.44% (rides coin-level bull swings even in a down year).** Tradeoff: higher drawdown (13–32% at 20%). Shipped default BULL_HOLD_PCT=0.15 (balanced), configurable. Caveats: only 2yr tested (regime-timing overfit risk), concentration if many coins bull at once, still below pure buy&hold in a raging bull (+36% vs +124%) — but far better than grid alone WITH downside protection. Now covers all 3 phases: BEAR defend · NEUTRAL grid stream · BULL ride. Committed 2439f57, deployed.
- **2026-08-03** — **OOS validation (5 years, fixed live config, bull-hold 15%): the system GENERALIZES — not overfit.** Per-year system vs buy&hold: 2022 −29.5% vs −67.8% (OOS, crypto winter — defended, cut loss in half), 2023 +49.6% vs +255% (OOS, recovery — bull mode rode it), 2024 +27.2% vs +124% (in-sample), 2025 +8.3% vs −8.5% (in-sample), 2026-YTD −9.3% vs −36.5%. On the UNSEEN 2022/2023 the behaviour matches in-sample character (positive in up years, cushioned in down years) → regime timing works out-of-sample, NOT fit to 2024/25. Full-cycle compound 2022→Aug2026: system ≈+32% vs buy&hold ≈+49%, but worst year −29.5% vs −67.8% and maxDD ~35% vs ~77% — ~2/3 the return at roughly half the drawdown, much smoother. Honest limits stand: deep bear still −30%, strong bull badly trails holding, drawdowns 19–35% in volatile years. Overfit concern resolved; it's a defensible risk-adjusted strategy.
- **2026-08-03** — **Tested two proposed improvements — BOTH make it worse; current config validated (no changes shipped).** (1) **Iteration frequency 1h vs 4h** (full system, 2024/2025): 4h +28.5%/+8.3% vs 1h −6.0%/+5.6% — 1h is noisier (SMA100≈4d), ~2× the churn, worse entries. 4h stays the sweet spot; the "nothing happens between 4h runs" cadence is correct. (2) **Trailing stop on the BULL hold** (transitions, 2022-2025): trail 8-20% HURTS, esp. 2023 bull +49.6%→+31% — trailing whipsaws out of healthy bull pullbacks and misses the continuation; exit-on-regime-flip (current) captures bulls better despite lag. Conclusion: transition logic + 4h cadence already near-optimal; recorded as negative results so we don't retry. Don't fix what isn't broken.
- **2026-08-03** — **Postgres storage backend + trade history** (durability + audit + reporting). New `src/store.py`: `PostgresStore` (portfolio row as JSONB + append-only `trades` table, autocommit, auto-creates schema) or `JsonStore` fallback (now ATOMIC via temp+rename; trades → trades.jsonl). `get_store()` picks Postgres when DATABASE_URL set, else JSON. `grid_broker` refactored onto the store + logs every buy/sell (bag & hold) to the trade log. `psycopg[binary]` added. Railway: added managed Postgres service, wired `DATABASE_URL=${{Postgres.DATABASE_URL}}` (internal) onto grid-stream. **Verified against the real Railway Postgres via a temporary TCP proxy: schema created, portfolio + 2 trades persisted correctly; then truncated test data and DELETED the public proxy (security — DB reachable only via internal URL now).** Also added a `🗄 backend` indicator to console + Telegram. Committed 2aad78e, deployed. Live bot now stores state+history in Postgres; local dev stays on atomic JSON.
- **2026-08-04** — **Tax model corrected (user caught it).** Was: backtest taxed `end_equity − start` = total prior incl. UNREALISED open bags/holds. Correct LT model: GPM 15% on REALISED gains only (closed trades), annually, above €500 — not per-trade, not on open positions. Fix: `BacktestResult.realized_pnl` (sum of SELL pnls) → `tax()` now taxes realised only. Live runner (which never deducted tax — good) now shows an **estimated tax LIABILITY** line (on realised, paid yearly) as separate info, NOT subtracted from equity — because equity is your live trading capital and open bags aren't taxable until sold. Console + Telegram show it. Committed 796f43e, deployed.
- **2026-08-05** — **LiveGridBroker built — real Binance execution.** `src/binance_trade.py` (signed spot client: get_free_balances, market_buy quoteOrderQty, market_sell with LOT_SIZE round-down, fill parsing — MARKET orders only, no resting/stops). `LiveGridBroker(GridBroker)` in grid_broker.py: same logical ledger (bags/holds in store) but orders hit real Binance; cash = real free USDT (source of truth); bag qty/cost from actual fills; `reconcile()` warns if exchange balances drift >5% from ledger. `get_grid_broker()` returns Live only when TRADING_MODE=live AND keys present, else paper. Runner wraps orders in try/except (a failed order logs + skips, no phantom bag) + runs reconcile in live. Verified SAFELY (no orders placed): paper still default (GridBroker/json); signed client reads real balance read-only; LiveGridBroker inits & reads balance. **NOTE: the real account currently holds 0 USDT** (funds in LTC/XRP/USDC/ARB/ENA) — live needs USDT funding first. Assistant did NOT enable live (stays paper). Committed 71cfce9, deployed. Pre-live checklist remains: fund USDT, disable withdrawals on key, add keys to Railway, set TRADING_MODE=live.
- **2026-08-05** — **Switched quote currency USDT → USDC** (EU/MiCA restricts USDT for EEA users; user holds USDC). Added `QUOTE_ASSET` config (default USDC); `binance_trade.free_quote()` reads it; base-asset stripping, reconcile, universe filter, and all report labels now use quote_asset. TARGET_COINS → BTCUSDC/ETHUSDC/BNBUSDC/SOLUSDC/XRPUSDC (verified all 5 have 4h data + prices). Also fixed hold sizing: added `broker.capital_base` (paper=start balance; **live=real quote balance read at init**) so bull-hold sizes off REAL capital — prevents an all-in hold when live capital ≪ paper reference; report % now vs capital_base too. Verified paper on USDC (labels show USDC, coins show BTC/ETH/…). Account has ~$21.78 USDC (0 USDT). Committed 7c1e133, deployed (still paper). For the ~$22 live pipeline smoke-test: grid $10 bags will fire (~2), bull-holds won't (0.15×22=$3.3 < $10 min-notional) — enough to verify execution plumbing, not the full strategy.
- **2026-08-05** — **24/7 LIVE on Railway (EU) — geo-block solved, first cloud live cycle ran clean.** Root cause of the earlier HTTP 451 was Railway's **US region** (Binance.com blocks US IPs); on Pro, moved grid-stream to **europe-west4 (Amsterdam)** + allocated **EU static IPs** (`208.77.244.242`, `152.55.184.240/241`), which the user whitelisted on the Binance key (spot=on, withdrawals=off, ipRestrict=on). Set live vars on Railway (`TRADING_MODE=live`, `GRID_UNIT_USDT=6`, `BULL_HOLD_PCT=0.30`, USDC). Migrated the local live-test state (BNBUSDC bag + ETHUSDC hold + journal) into **Railway Postgres** via a temporary TCP proxy (deleted after, DB not public). Temporarily accelerated cron to 5-min to verify the first EU cycle, then reverted to `0 */4`. **Cloud cycle logs confirmed: `mode=LIVE`, `postgres+LIVE`, ZERO 451, no crash** — reads+run work from EU. **Caught & fixed a data-integrity issue:** a **phantom BTCUSDC bag** appeared in Postgres (equity showed +199%). Diagnosed by shape — `cost=6.0` exact + fee-adjusted `qty=(6−6·0.001)/px` = **paper-broker formula**, whereas the real BNB bag had `cost=5.9628` (actual cummQuote fill). Cause: during the live-deploy rollover, the 12:45 cron tick fired on the **still-running OLD paper deployment**, which wrote a paper bag to the shared Postgres; the new LIVE deploy then inherited it. Binance confirmed **0 BTCUSDC orders ever placed**. Removed the phantom bag + its journal row; ledger now matches the exchange exactly (cash $9.28, BNB ×1, ETH hold). **Code fixes:** `LiveGridBroker.capital_base = cash + invested cost` (correct %-baseline, not just free cash); **defined `BrokerError`** (was an undefined name — latent NameError); live `buy`/`buy_hold` now **raise on an unfilled order** (`qty<=0`) so a no-fill can never leave a phantom bag. Commits a080f21, f0f567e. NEXT: watch the natural 16:00 UTC cron for the first all-live (no rollover) cycle; a real cloud ORDER (POST /order from EU IP) is still to be observed — signed GET already works from EU, so confidence is high.
- **2026-08-06** — **First ~24h of live reviewed. Real cloud ORDER confirmed + fixed a TP-sell blocker.** ✅ The cloud placed a **real BTCUSDC buy** from the EU IP (bag entry 64380.73, ~$5.79; real USDC cash dropped $9.28→$3.49 as read by the live broker) — so POST /order from Railway EU works, the last unverified piece. Book after 24h: BTC bag $6 (TP 64767), BNB bag $6 (TP 599.86), ETH hold $7 (BULL, +2.1%), cash $3.49, realized **$0**, equity $21.94 (+0.71%, all from ETH-hold drift). ❌ **BUG caught in the logs:** two TP **sells FAILED** (`⚠ORDER-FAILED`) — BNB at 599.99 and BTC at 64796, both above their TP. Cause: Binance charges the buy **fee in the base asset**, so the ledger bag qty (e.g. BNB 0.01) is a hair MORE than the real free balance (0.00999); `market_sell(ledger_qty)` → Binance -2010 "insufficient balance" → reject. Without this, the stream can BUY but never REALIZE profit. **Fix (commit 3cdcb09):** `LiveGridBroker._sellable(symbol, want)` reads the real free base balance and sells `min(want, free)`; used in both `sell_bag` and `sell_hold` (round_qty still floors to LOT_SIZE). Failed sells left bags intact (exception path), so ledger still matches the exchange — next cycle at/above TP will now sell. Also noted: local machine now gets Binance **401 (-2015)** — home IP no longer whitelisted (only the 3 EU static IPs remain), so all live inspection is via Railway logs + Postgres, not local Binance calls.- **2026-08-08** — **Diagnosed recurring `⚠ORDER-FAILED` in the live logs — it's the BTCUSDC TP-sell, still failing AFTER the `_sellable` fix.** By cycle-timestamp: BTC was clean 08-06→08-07 08:00, then FAILS every 4h cron from **08-07 12:00 through 08-08 08:04**. The fails begin exactly when BTC rose above the bag's TP (64767) — so the bag reached take-profit, the bot tries to sell it each cycle, and Binance rejects. `realized` is still **$0** — no bag has EVER sold in live. Since these fails are all POST `3cdcb09`, the free-balance cap did **not** cover BTC — most likely the ~$5.8 bag sits right on Binance's `minNotional=$5` (`applyMinToMarket:true`), and after `_sellable` caps to real free base minus buy-fee and `round_qty` floors DOWN to `LOT_SIZE` (0.00001 BTC), the sellable notional drops under the floor → reject. Couldn't confirm the exact Binance code locally (home IP gets `-2015`, not whitelisted) and the full error text only went to Telegram, truncated. **Change (this session):** `main.py` now `print()`s the full `type(exc).__name__: exc` to stdout on any order failure, so Railway logs reveal the exact code/msg (a redeploy runs one cycle on container start → instant reveal, no 4h wait). NEXT: read the code from the new cycle, then fix (likely: on TP-sell sell the full aggregate free base balance, and/or raise BTC bag size well above min-notional).
- **2026-08-08** — **Root-caused & fixed the recurring BTCUSDC TP-sell failure — it was Binance `-1100` (scientific-notation qty), NOT a balance issue.** Added full-exception stdout logging (prev session's diag change) and force-ran one cycle (temp `*/5` cron) to reveal the exact error: `HTTP 400 code -1100 "Illegal characters found in parameter 'quantity'"`. Cause: `binance_trade.round_qty()` returns a **float**; a tiny qty like the ~$6 BTC bag (`0.00009` BTC at ~$65k) stringifies to `'9e-05'`, and the `'e'` violates Binance's `^([0-9]{1,20})(\.[0-9]{1,20})?$` regex. So **every** BTC TP-sell was silently rejected since it first crossed TP (08-07 12:00) — the live stream could BUY but never REALIZE (realized stuck at $0 the whole run). BNB (qty ~0.01 → `'0.01'`) stringified cleanly, which masked it. The earlier `_sellable` fix (3cdcb09) addressed a *different*, real cause (-2010) but couldn't touch this. **Fix (commit a2ff33f):** new `qty_str(symbol, qty)` formats the LOT_SIZE-rounded qty as a **plain fixed-point string** (`f"{q:.{places}f}"`); `market_sell` now sends that. Verified locally (`0.00009` not `9e-05`) and **LIVE**: forced cycle at 09:40 UTC sold the BTC bag (`bags 1→0`, `SOLD 1`), **realized $0.00 → +$0.05** — first realized profit of the live run, zero fails. Reverted cron `*/5 → 0 */4`. Diag commit 53a47b2, temp-cron ccd9dd0, fix a2ff33f, revert (this commit).
- **2026-08-08** — **Hardened the whole codebase against the -1100 (sci-notation) class + added a regression test; verified locally.** After the live BTC fix, swept every place a number is sent to Binance so this can't recur: (1) `binance_trade.market_buy` now sends `quoteOrderQty` as `f"{x:.2f}"` (was a bare float); (2) the legacy `broker.py` `LiveBroker.buy/sell` (early SMA-MVP, not used by grid main.py but still in the tree) now send plain strings too — `buy` formats the quote, `sell` reuses the shared `binance_trade.qty_str()` as the single source of truth; removed its now-dead `_round_lot`. **New `tests/test_order_formatting.py`** (4 tests, standalone `python tests/test_order_formatting.py`, no pytest needed): asserts `qty_str` output matches Binance's exact regex `^([0-9]{1,20})(\.[0-9]{1,20})?$` and never contains `e`/`E` across 6 LOT_SIZE steps (1e-8 … 1 dp) × tiny+normal qtys, pins the floor-rounding, and locks the exact pre-fix regression (`str(0.00009)=='9e-05'` → `qty_str=='0.00009'`). All 4 pass. **Local end-to-end paper run** (STATE_DIR→scratchpad, no DB/Telegram) fetched all 5 coins, detected regimes (ETH BULL→hold-buy, rest NEUTRAL→grid buys), placed paper bags, computed equity — zero errors. Grepped `src/` for any other float→order param: only the two order fns + legacy broker existed, all now covered.

- **2026-08-08** — **Reworked the cycle report output (Telegram + stdout) for readability.** After the live stream started realizing profit, the per-cycle report was cleaned up. `main.py`: (1) **positions now render as a monospace, column-aligned table** (`Монета · Поз. · Влож · PnL% · Ждёт`) via new `_pos_table()` — wrapped in `<pre>` for Telegram, printed raw to logs; emoji moved OUT of the table (double-width, broke column alignment). (2) `_fmt_price()` gives compact prices across magnitudes (BTC `64,000` / ETH `2450.00` / XRP `1.0412`). (3) **Order failures + reconcile drift split into their own `errors[]` section** (`⚠️ Ошибки за цикл`) instead of being mixed into the actions list. (4) `_tax_line()` prints a dynamic Lithuania GPM estimate (15% above the €500/yr allowance), recomputed each cycle from current realized. (5) Telegram message re-laid-out: per-coin trend as an emoji legend (🟢/⚪/🔴), explicit 'без сделок' line when no trades fired. Smoke-tested the helpers (table aligns, tax correct in both below/above-allowance branches). Committed + deployed to Railway (live).

- **2026-08-08** — **Rebranded the Telegram bot identity to match the current grid-stream reality (via Bot API, no code change).** The bot `@CryptoAgentTradeBot` still carried the OLD project's metadata (name 'Crypto Agent | Spot Catcher', an AI/Revolut/cross-exchange-signals description, and dead menu commands `/start /backtest /patterns /mode /status` — none of which the push-only notifier handles). Set via `setMyName` / `setMyDescription` / `setMyShortDescription` / `deleteMyCommands`: name → **🌊 Grid-Stream**; overview (EN) → automated spot bot skimming micro-profits on pullbacks across a blue-chip basket (BTC/ETH/BNB/SOL/XRP in USDC), adding on dips, holding through bull trends, posting a report every 4h, 24/7; short desc (EN) → one-liner of the same; **menu commands cleared** (the app is a Railway cron push-notifier with no update listener, so any menu command was cosmetic/dead). Note: the `@username` is BotFather-only and unchanged; overview shows only on the empty-chat screen for new users. User chose: name 🌊 Grid-Stream, remove commands, English descriptions.

- **2026-08-11** — **reconcile drift now has an absolute-USD floor (kills harmless dust warnings) + dropped XRP from the basket.** User saw `⚠️ Ошибки за цикл: 🔎 reconcile: BNB: ledger 0.009000 vs exchange 0.009915` in Telegram and asked what was wrong. Diagnosed: NOT a crash (logs clean — zero tracebacks/API errors over the retained ~466 lines; deploy SUCCESS; cron `0 */4` exits between runs = normal). It's the read-only `LiveGridBroker.reconcile()` safety monitor firing on a **safe-direction** drift — exchange had ~0.000915 BNB MORE than the ledger (~$0.55 of untracked pre-existing BNB dust); the old check was pure-relative (`>5%`) so sub-dollar dust on a tiny $5 bag tripped it (0.000915/0.009 = 10.2%). Direction is the harmless one (surplus, not deficit — the phantom-bag danger is the opposite; `_sellable()` clamps sells so surplus can't hurt). **Fix (`grid_broker.py`):** reconcile now requires BOTH `> RECONCILE_REL` (5%) AND `> RECONCILE_ABS_USD` ($1) using the `prices` it already receives (previously unused); warning line now also prints `(~$X.XX)`. Smoke-tested via unbound-method stubs: $0.55 dust → silent, $5.44 & $1.10 drift → still warn. Existing `tests/test_order_formatting.py` still 4/4. Also (user: "убери XRP, он нам не нужен") **removed XRP from the universe** — it had 0 open bags so removal is clean: set Railway `TARGET_COINS=BTCUSDC,ETHUSDC,BNBUSDC,SOLUSDC` (verified read-back), updated local `.env`, and dropped `XRPUSDT` from the `config.py` static fallback. Live bot picks up the new basket on the next cron tick. NOT yet done: Telegram overview/short-desc still lists XRP (marketing copy) — left as-is unless user wants it re-synced. Realized stream at −0.35 USDC (real small stops on BNB/ETH regime flips) + a ~−1.5% *unrealised* equity dip from BTC 65.2k→63.9k are separate/expected, not bugs.

- **2026-08-11** — **Telegram trend/counts lines redesigned — dropped the circles + legend (user: "неинтуитивно, убрать кружки").** The old block was three noisy lines: `🧺 3 меш · 🟢 0 hold` (green circle decorating the hold count — collided semantically with the green=BULL used below), `🧭 Тренд: BTC ⚪️ · ETH ⚪️ · …` (a same-looking circle per coin), and a `🟢 рост · ⚪ флэт · 🔴 падение` legend you had to cross-reference to read the trend at all. Replaced (`main.py`) with plain-word grouping: new `_trend_summary(regimes)` buckets coins by state and prints only non-empty groups — `📈 Растут: ETH` / `➡️ Боковик: BTC, BNB` / `📉 Падают: SOL` — collapsing to a single `🧭 Тренд: все в боковике` when every coin shares one state (the common case). Directional emoji (📈➡️📉) now label the *group* once and read on their own, so the legend line is gone entirely. Counts line de-noised to `🧺 Мешков: 3 · 💼 Холдов: 0` (green circle removed, briefcase for holds). Removed the now-unused `_REG_EMOJI` map (console table still uses `_REGIME_ICON`). Verified all three render paths (all-flat / mixed / all-bull); import clean, tests 4/4.

- **2026-08-11** — **Backtested idea #1 (pool take-profit: sell the whole stack at avg-cost+TP) — REJECTED, it's clearly WORSE than the current per-bag TP. No live change.** Hypothesis (user's goal: make bought bags convert to "feast" more reliably): closing the entire bag-stack once price recovers to the cost-weighted AVERAGE entry +0.6% would let a small bounce release all frozen bags at once (winners carry laggards) → faster feast. Implemented behind a flag: `run_grid(pool_tp=...)` in `backtest/scalp.py` (default False = unchanged, back-compat with grid_screen + tests) and a new comparison harness `backtest/avg_tp_test.py` (live basket BTC/ETH/BNB/SOL on USDT for depth, live cfg TP0.6/step0.5/SMA100/unit10/max40, ~2.7y of 4h = 6000 candles, continuous + per-year OOS reset, baseline vs pool side-by-side). **Result (basket sum, continuous 2023-11→2026-08): BASELINE stream +580 / equityΔ +119 / feastΔ +585 vs POOL stream +318 / +17 / +323 — pool banks ~45% LESS stream and less feast on EVERY coin and EVERY year.** Only upside: pool's max-drawdown was marginally lower (e.g. SOL 7.1%→5.9%, ETH 5.1%→3.7%) and ~half the trades. **Why the intuition was wrong:** the grid's edge IS the high-frequency per-bag churn — each bag independently scalps any local +0.6% bounce off its OWN entry. Pool-TP forgoes all those intermediate scalps and only fires when price clears the (much higher) average of a deep stack, so it waits far longer and harvests far less. "Winners carry laggards" is real but costs the whole per-bag harvest, which dominates. Conclusion: the EXIT mechanism isn't the weak point — per-bag TP is already good. The real lever for "bags more likely to become feast" is ENTRY quality (idea #2: only add bags on genuine oversold/mean-reversion, not every −0.5% dip) → open fewer but better bags so fewer freeze. Recorded as a negative (project discipline: don't re-try). NEXT: backtest idea #2. Flag code kept (off) as a reusable test artifact.

- **2026-08-11** — **Idea #2 (oversold entry gate) REJECTED; then a geometry sweep found the REAL efficiency lever — WIDEN THE GRID STEP.** (1) Entry-gate test (`backtest/entry_gate_test.py`, live basket, ~2.7y 4h): RSI14<40/35/30, EMA20−1%/−1.5%, up-tick bar, combos — all vs BASE (uptrend-only). **All oversold filters cut stream FAR more than risk** (RSI<35: stream +580→+62 = −89%, bags −77%, equity went NEGATIVE); on stream-per-drawdown efficiency BASE=129, up-tick=130 (only one matching base), oversold=69–80 (worse). "Fewer but better bags" disproved — the filters skip the very dips the grid profits from. Reconfirms: deployment% dominates, not entry cleverness. (2) Added stagnation metrics to `run_grid` (avg/max bag hold-time in bars, oldest open-bag age, time-avg underwater-capital %) and ran `backtest/geometry_sweep.py` over step∈{0.5,0.8,1.2} × TP∈{0.6,1.0} × maxBags∈{40,20}. **Finding: widening step 0.5→0.8→1.2% keeps ~88–97% of the EQUITY (real money: +121→+112→+106) while cutting max-drawdown 4.5%→3.7%→3.0% AND underwater-capital 5.30%→4.41%→3.60%, same ~6.6d per-bag turnover.** Raw stream drops (580→489→408) but equity barely moves — fewer, deeper, better-spaced bags are more efficient per dollar. Best risk-adjusted cells: `step 0.8/TP 0.6/max 40` (stream +489, eq +112, dd 3.7%, frozen 4.4%) or `step 1.2/TP 0.6/max 40` (eq +106, dd 3.0%, frozen 3.6%). maxHold 515d / endAge 569d were CONSTANT across all configs = a few deep structurally-stranded bags every geometry holds (widening step avoids stacking MORE around them, can't rescue those). **Caveat:** sweep uses $2500 start / $10 unit; the LIVE bot is $6 unit on ~$21 (affords only ~3 bags before cash-out), so step barely bites until the account is funded to hundreds+. Recommendation to user: widen `GRID_STEP_PCT` 0.5→0.8 (balanced) — first change in 3 tests that actually improves the stated goal (stream stays, bags stagnate less). Nothing applied yet. Test artifacts kept.

- **2026-08-11** — **Built path B: dynamic bag-sizing (bag = % of live balance, auto-scales on deposit) + deposit/withdrawal detection. Enabled at 0.4% (no-op at current $22, future-proofs a top-up).** User asked whether the bot uses a larger balance and adjusts if USDC is topped up (e.g. $22→$500). Clarified the two actors: *Claude does NOT monitor* (only acts in-session); the *Railway cron app* auto-USES more free USDC each cycle (`free_quote()` is the cash source of truth) but did NOT auto-scale bag SIZE — grid `unit` was a fixed $6 while bull-holds already scaled (30%×capital_base), so a big deposit would run tiny $6 grid bags + huge holds (imbalanced). **Feature:** `config.grid_unit_pct` (+`grid_min_unit` floor); `grid.effective_unit(capital_base)` = `max(min_unit, pct×capital_base)` when pct>0 else fixed; `main.py` computes it after broker init and `dataclasses.replace`s it into the cycle params (threads into buys + plan_actions cash-check), prints a `bag sizing` line. **Deposit detector:** `LiveGridBroker` captures `stored_cash` (last persisted) before overriding with live `free_quote()`; `external_flow = live − stored` = deposits/withdrawals since last cycle (own orders were baked into stored_cash); `main.py` shows `💰 Пополнение +$X` / `🏧 Вывод −$X` in console+Telegram when |flow| > max($1, 0.5%). Base `GridBroker.external_flow=0.0` for paper. **Backtest** (`backtest/sizing_test.py`, run_grid gained `unit_pct`/`min_unit`; live basket ~2.7y at starts $22/$500/$5000 × fixed/0.4%/1%/2%): the min-notional $6 floor PINS small accounts — $22 and $500 run $6 bags at ANY pct ≤1% (only 2% lifts $500→$10); dynamic sizing only enlarges bags once pct×balance>$6, i.e. ~$1500+. At $5000 it works cleanly: fixed $6→+0.33% equity (capital wasted) vs 0.4%$20→+1.04%, 1%$50→+2.36%, 2%$100→+3.85% (dd 1.1%→3.9%→9.5%→18.7% — pct is a risk dial). $22 is structurally over-deployed regardless (dd 58%, frozen 160%) — only more capital fixes that. **Decision:** enabled `GRID_UNIT_PCT=0.004` + `GRID_MIN_UNIT=6` on Railway + .env. At current $22 this is a NO-OP (floored to $6 = today's behaviour); the moment the account grows past ~$1.5k, bags auto-scale to 0.4% (the validated ~16%-max-deploy MANY profile). pct is left as the risk dial to raise when funded. All committed; existing tests 4/4, main imports clean.

- **2026-08-11** — **Dynamic-sizing backtest over the Trump-inauguration window ($22/$500/$5000/$10000).** Extended `backtest/sizing_test.py` with a `since`-date slice (default `2025-01-20`, block computed on full history then sliced so the SMA100 gate stays warm) + added the $10k start. Window 2025-01-20→2026-08-11 (~19mo, a grid-bear). **Result confirms the feature's whole point — balance-invariance:** at the live 0.4% sizing, $5000 and $10000 give IDENTICAL % outcomes (stream +2.14%, equity −1.54%, dd 3.7%, frozen 4.95%) because the bag scales $20→$40; fixed $6 instead WASTES capital as balance grows ($10k fixed = +0.32% stream, half of $5k's). Small accounts pin to the $6 floor: $22 is catastrophic (equity −39%, dd 55%, frozen 94% — a $6 bag is 27% of the account), $500 runs $6 bags at ≤1% (only 2% lifts to $10). pct stays the risk dial: at $5k, 0.4%→2% scales stream +2.1%→+10.6% and dd 3.7%→18.1%. **Two honest caveats recorded:** (1) this harness is the GRID SLEEVE ONLY (uptrend-gated) — it does NOT model the BULL buy-&-hold overlay or BEAR-no-buy that live in the runner, so absolute equity is understated (per earlier OOS, the full system was +8.3% in 2025 vs grid-only negative); (2) the window is a grid-bear so stream is +ve everywhere but mark-to-market equity is −ve from frozen bags. Offered (not yet built) a full-runner backtest (with holds + regime) for true equity numbers. Committed. **Session close: parking the crypto project here** — live bot healthy on Railway with this session's changes (reconcile $1 floor, XRP dropped, report trend-lines redesigned, GRID_STEP_PCT 0.5→0.8, dynamic 0.4% sizing + deposit/withdrawal detection); moving to another project next.

- **2026-08-13** — **Live-results deep-dive + report UX overhaul + corrected a phantom sell in the ledger.** User asked "how are we doing — are we actually in profit?". Read the live Postgres `trades`/`portfolio` directly (via a throwaway TCP proxy on the Postgres service, removed after). **Finding:** 8-day live realized was −0.15 USDC, but 8 of 9 sells were clean grid-TP (all small green, +0.7…+1.2% net of fees); the entire lifetime loss was ONE artefact — a phantom BNB bag from the 08-05 paper→live flip liquidated 08-08 at −0.5225 (−8.8%, grid never voluntarily sells at a loss). Since 08-09 every sell is green. So the strategy is quietly net-positive; the "loss" was a migration ghost. **Three changes (report-only, no trading logic touched):** (1) `store.py` — new `recent_sells(limit=50)` on both backends (Postgres: `SELECT … WHERE side='SELL' ORDER BY ts DESC`; JSON: filter+tail of `trades.jsonl`). (2) `main.py` — new `_sells_message()` builds a **second Telegram message**: a monospace win/loss ledger of the last 50 sells (date · coin · profit USDC · % · ✅/❌, mark placed LAST so the double-width emoji doesn't shift columns) with a `🟢 в плюс N · 🔴 в минус M · итог ±X` header; sent right after the main report, skipped when there are no sells. (3) **Blunter, layperson-friendly headline block** (user: current indicators unclear) — `📊 Счёт/💵 Ручеёк/🎉 Пир/🧺 Мешков` → `💰 Всего на счету N (свободно/в закупках)`, `💵 Заработано всего ±X — ✅ в плюсе/❌ пока в минусе`, `🎯 Если все закупки отработают N`, `🛒 Открытых закупок N`; trend collapses to plain `🧭 Рынок: спокойный, боковик 😴 / растёт 📈 / падает 📉`. (4) **Ledger correction (per explicit user request "убери учёт той теневой продажи"):** atomically DELETEd the single phantom BNB sell (id=11, pnl −0.5225) and backed its loss out of `portfolio.realized` (−0.1489 → **+0.3736**); verified 0 phantom rows remain, corrected history now reads 7 green / 1 red / итог +0.37. The remaining −0.01 ETH is a legit small hold-exit, kept. Existing tests 4/4; both messages rendered against live data before shipping.
